<%= form_for(@snap) do |f| %>
  <% if @snap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@snap.errors.count, "error") %> prohibited this snap from being saved:</h2>

      <ul>
      <% @snap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :reception_time %><br>
    <%= f.datetime_select :reception_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
